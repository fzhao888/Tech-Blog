{{#each comments as |comment|}}

<div class="row comment-header"></div>
<div class="row display-comment">
  <p class="comment-message"> {{comment.message}} </p>
  <p>Posted by {{comment.user.username}} on {{format_date date_created}}</p>
</div>

{{/each}}